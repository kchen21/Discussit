<% url = @post.url %>

<h2><%= @post.title %></h2>

<label for="url">URL</label>
<p id="url"><%= url == "" ? "None" : url %></p>

<%= @post.body %>

<%= link_to "Edit post", edit_subforum_post_url(@post) %>
<%= link_to "Go back to subforum", subforum_url(@subforum) %>

<h3>Comments</h3>

<%# TODO: Create a link to new_comment_url %>
<%# TODO: Create a list of links to each of @post's comments %>
